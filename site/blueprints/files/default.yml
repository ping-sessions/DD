title: File
columns:
  main:
    width: 1/1
    sections:
      content:
        type: fields
        fields:
          tags:
            label: Tags
            type: checkboxes
            width: 1/2
            options: query
            query:
              fetch: site.find('home').words.split
              text: "{{ arrayItem.value }}"
              value: "{{ arrayItem.value }}"
          text:
            label: Caption/Text Snippet
            width: 1/2
            type: textarea
            size: medium
          author:
            width: 1/2
            type: text
            size: small